/** Builds a deterministic greeting for the supplied name. */
export class /*@case.greeter.definition.start*/Greeter/*@case.greeter.definition.end*/ {
  greet(name: string): string {
    return `Hello, ${name}`
  }
}
